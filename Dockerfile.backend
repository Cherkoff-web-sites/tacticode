# Backend для Timeweb: сборка из корня репо
# Использовать при создании Backend-app с типом Docker
FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm install --production

COPY backend/ ./
CMD ["node", "src/server.js"]
